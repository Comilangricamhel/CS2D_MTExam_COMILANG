let favoriteColors = []; // empty array

// the  following just prompt user to add three colors
  let color = prompt("First a color you like:");
  console.log(favoritecolor);
  
  favoriteColors.push("Second a color you like");
  console.log(favoritecolor)
  // Log the updated array to the console
  console.log("Updated color list:", favoriteColors);
}
